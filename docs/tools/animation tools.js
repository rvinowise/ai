


function stop_till_enter(root) {
	root.stop()
	
	//window.onkeydown = (key_event) => {
	//	if (key_event.code == "Space") {
	//		root.play();
	//	}
	//}
}

// function stop_till_enter(root) {
	// root.stop()
	
	// window.addEventListener("keydown", keyPressed);

	// function keyPressed(key_event) {
		// if (key_event.key.toLowerCase() == "enter") {
			// root.play();
		// }
	// }
// }