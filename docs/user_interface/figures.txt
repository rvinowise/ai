1. menu: create a figure2. spot selection mode (figure icon is attached to the mouse cursor)3. clicking on the spot places a figure in it, with an empty representation. the representation is selected as the "constructed one", and the scene switches into the figure building mode.4. drag-n-drop figures from the storage creates subfigures that should be placed at the representation