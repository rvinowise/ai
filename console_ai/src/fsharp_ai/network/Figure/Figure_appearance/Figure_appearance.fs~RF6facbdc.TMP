namespace rvinowise.ai


type Figure_appearance(head, tail) = 
    member _.head: int = head
    member _.tail: int = tail

    new (figure: string, head, tail) =
        Figure_appearance(head, tail)

